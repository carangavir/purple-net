import { listTemplates } from "@/server/campaigns/service"; import { createTemplateAction } from "./actions";
export default async function TemplatesPage() { const rows = await listTemplates(); return <section><p className="text-sm font-bold tracking-wider text-[var(--purple)]">PHASE 7</p><h1 className="text-3xl font-bold">Templates</h1><form action={createTemplateAction} className="mt-5 grid gap-2 rounded border bg-white p-4"><input name="name" required placeholder="Template name" className="border p-2"/><input name="category" required placeholder="Category" className="border p-2"/><input name="subject" required placeholder="Subject" className="border p-2"/><textarea name="body" required placeholder="Body; use merge fields only after review" className="border p-2"/><button className="w-fit rounded bg-[var(--purple)] px-3 py-2 text-white">Save template</button></form><ul className="mt-5">{rows.map((template) => <li key={template.id} className="border-b p-3"><b>{template.name}</b> · {template.category}</li>)}</ul></section>; }
