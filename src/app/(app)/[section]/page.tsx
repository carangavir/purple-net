import { notFound } from "next/navigation";
const pages: Record<string, { title: string; description: string }> = {
  dashboard: { title: "Dashboard", description: "Your recruiting overview will appear here." }, prospects: { title: "Prospects", description: "Prospect management begins in a future phase." }, schools: { title: "Schools", description: "School records begin in a future phase." }, directors: { title: "Directors", description: "Director records begin in a future phase." }, visits: { title: "Visits", description: "Visit planning begins in a future phase." }, campaigns: { title: "Campaigns", description: "Campaign workflows begin in a future phase." }, tasks: { title: "Tasks", description: "Task management begins in a future phase." }, reports: { title: "Reports", description: "Reporting begins in a future phase." }, imports: { title: "Imports", description: "Workbook imports are intentionally deferred to Phase 2." }, templates: { title: "Templates", description: "Template management begins in a future phase." }, settings: { title: "Settings", description: "Application settings begin in a future phase." },
};
export function generateStaticParams() { return Object.keys(pages).map((section) => ({ section })); }
export default async function PlaceholderPage({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; const page = pages[section]; if (!page) notFound(); return <section className="max-w-3xl"><p className="mb-2 text-sm font-bold tracking-wider text-[var(--purple)]">PHASE 1</p><h1 className="text-3xl font-bold">{page.title}</h1><div className="mt-6 rounded-lg border border-[var(--border)] bg-white p-5"><h2 className="font-semibold">Phase 1 placeholder</h2><p className="mt-2 text-[var(--muted)]">{page.description}</p></div></section>; }
