import { listTemplates } from "@/server/campaigns/service"; import { createCampaignAction, createDraftAction } from "./actions";
export default async function CampaignsPage() { const templates = await listTemplates(); return <section><p className="text-sm font-bold tracking-wider text-[var(--purple)]">PHASE 7</p><h1 className="text-3xl font-bold">Campaigns</h1><form action={createCampaignAction} className="mt-5 flex flex-wrap gap-2 rounded border bg-white p-4"><input name="name" required placeholder="Campaign name" className="border p-2"/><input name="objective" placeholder="Objective" className="border p-2"/><select name="templateId" className="border p-2"><option value="">No template</option>{templates.map((template) => <option key={template.id} value={template.id}>{template.name}</option>)}</select><button className="rounded bg-[var(--purple)] px-3 text-white">Create campaign</button></form><h2 className="mt-8 text-xl font-bold">Prepare Outlook draft</h2><p className="mt-1 text-sm text-[var(--muted)]">This opens Outlook compose for your review; Purple Net does not send email.</p><form action={createDraftAction} className="mt-3 grid gap-2 rounded border bg-white p-4"><input name="recipient" type="email" required placeholder="Recipient email" className="border p-2"/><input name="subject" required placeholder="Subject" className="border p-2"/><textarea name="body" required placeholder="Draft body" className="border p-2"/><button className="w-fit rounded bg-[var(--purple)] px-3 py-2 text-white">Open reviewed Outlook draft</button></form></section>; }
