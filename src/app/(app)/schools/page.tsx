import Link from "next/link";
import { createSchoolAction } from "./actions";
import { listSchools } from "@/server/crm/service";
export default async function SchoolsPage({ searchParams }: { searchParams: Promise<{ q?: string; archived?: string }> }) { const params = await searchParams; const rows = await listSchools(params.q ?? "", params.archived === "1"); return <section><p className="text-sm font-bold tracking-wider text-[var(--purple)]">PHASE 3</p><h1 className="text-3xl font-bold">Schools</h1><form className="mt-5 flex gap-2"><input name="q" defaultValue={params.q} placeholder="Search schools" className="rounded border border-[var(--border)] px-3 py-2"/><button className="rounded border px-3 py-2">Search</button></form><form action={createSchoolAction} className="mt-5 grid gap-2 rounded border bg-white p-4 sm:grid-cols-5"><input name="name" required placeholder="School name" className="rounded border p-2"/><input name="metro" placeholder="Metro" className="rounded border p-2"/><input name="district" placeholder="District" className="rounded border p-2"/><input name="uilConference" placeholder="UIL conference" className="rounded border p-2"/><button className="rounded bg-[var(--purple)] p-2 font-semibold text-white">Add school</button></form><div className="mt-5 overflow-x-auto rounded border bg-white"><table className="min-w-full text-sm"><thead><tr className="text-left"><th className="p-3">School</th><th>Metro</th><th>District</th><th>Tier</th></tr></thead><tbody>{rows.map(({ school, metro, district }) => <tr key={school.id} className="border-t"><td className="p-3"><Link className="font-semibold text-[var(--purple)] underline" href={`/schools/${school.id}`}>{school.name}</Link></td><td>{metro?.name}</td><td>{district?.name}</td><td>{school.priorityTier}</td></tr>)}</tbody></table></div></section>; }
